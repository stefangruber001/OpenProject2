# Copy this file to `.env` and fill in real values. NEVER commit `.env`.
# See docs/runbook.md for how to get a local database running.

# --- Database (PostgreSQL) ---
# Local dev example (matches docker-compose in the runbook):
DATABASE_URL="postgresql://openproject:openproject@localhost:5432/openproject?schema=public"

# --- App ---
# Public base URL of the web app.
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# --- Observability (optional for local dev) ---
# SENTRY_DSN=""
