# Fill these in with your own Apple account details.
# See ios/README.md and the Beta Onboarding PDF for where to find each value.

app_identifier("com.caneisubirats.erp")   # must match PRODUCT_BUNDLE_IDENTIFIER
apple_id(ENV["APPLE_ID"])                  # your Apple ID email (App Store Connect login)
itc_team_id(ENV["ASC_TEAM_ID"])            # App Store Connect team id (optional)
team_id(ENV["DEVELOPER_TEAM_ID"])          # Apple Developer (portal) team id
