node_modules
.next
.turbo
dist
coverage
pnpm-lock.yaml
**/generated/**
playwright-report
test-results
out
# template placeholders ({{X}}) are not valid YAML flow maps — keep verbatim
tenants/_template

# Generated by `pnpm --filter @repo/erp-browser build` and committed on purpose
# (pages.yml publishes site/** with a bare checkout — no Node available there).
# MUST stay ignored: lint-staged runs prettier over *.js/*.cjs on commit, which
# would reformat these artifacts and make the CI drift check fail permanently.
site/erp-factory.js
site/erp-factory.cjs
