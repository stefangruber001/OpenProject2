---
description: Load App Producer from the Template repo and ship this web app to iOS
---

Add my Template repo `stefangruber001/Template` and copy its agents into
`.claude/agents/`, then use the **App Producer** agent to ship this web app to
iOS (premium native shell → TestFlight → autonomous tests). Pull the latest
learnings ledger before you start.
