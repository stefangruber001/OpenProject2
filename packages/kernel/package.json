{
  "name": "@repo/kernel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Factory kernel: tenancy spec, config resolution, ports, effective dating, money, events. Zero jurisdiction/sector knowledge.",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing/contracts.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^26.1.2",
    "eslint": "^10.8.0",
    "typescript": "7.0.2",
    "vitest": "^4.1.10"
  }
}
