{
  "name": "@repo/capability-extraction",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "description": "Generic document-field extraction: turns recognised text into candidate fields with confidence, provenance and arithmetic consistency checks. All locale knowledge (number/date formats, tax-id shapes, keywords, expected rates) arrives through a pack-supplied profile port. No jurisdiction or sector knowledge.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/kernel": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^26.1.2",
    "eslint": "^10.8.0",
    "typescript": "7.0.2",
    "vitest": "^4.1.10"
  }
}
