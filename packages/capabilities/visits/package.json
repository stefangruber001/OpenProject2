{
  "name": "@repo/capability-visits",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Generic site-visit capability: capture a visit with notes, room measurements and photo references, linked to a customer/lead, with area roll-up. No jurisdiction or sector knowledge.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/kernel": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^26.1.1",
    "eslint": "^10.7.0",
    "typescript": "7.0.2",
    "vitest": "^4.1.10"
  }
}
