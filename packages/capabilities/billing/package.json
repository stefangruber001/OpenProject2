{
  "name": "@repo/capability-billing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Generic billing capability: immutable invoices, numbering, rectification, tax via ports. No jurisdiction or sector knowledge.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/kernel": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^22.15.3",
    "eslint": "^9.39.1",
    "typescript": "5.9.2",
    "vitest": "^3.0.5"
  }
}
