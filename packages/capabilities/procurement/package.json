{
  "name": "@repo/capability-procurement",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Generic procurement capability: purchase orders with lines per chapter/project, a draft→sent→received lifecycle, committed-cost rollup by chapter and over-budget detection. No jurisdiction or sector knowledge.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/kernel": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^26.1.1",
    "eslint": "^10.7.0",
    "typescript": "7.0.2",
    "vitest": "^4.1.10"
  }
}
