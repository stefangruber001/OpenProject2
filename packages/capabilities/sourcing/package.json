{
  "name": "@repo/capability-sourcing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "description": "Generic sourcing capability: multi-bidder price comparison over quantified lines — dated/sourced prices, per-group totals, variance vs baseline, missing-price visibility, explicit selection. No jurisdiction or sector knowledge.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/kernel": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^26.4.0",
    "eslint": "^10.9.1",
    "typescript": "7.0.2",
    "vitest": "^4.1.11"
  }
}
