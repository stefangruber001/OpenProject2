{
  "name": "@repo/capability-catalogue",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "description": "Generic catalogue capability: a register of work/material items (code, chapter, unit, cost/price) and room templates that expand into quote lines. No jurisdiction or sector knowledge.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/kernel": "workspace:*",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^26.4.1",
    "eslint": "^10.10.0",
    "typescript": "7.0.2",
    "vitest": "^5.0.0"
  }
}
