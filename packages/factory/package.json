{
  "name": "@repo/factory",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The factory: pack registry, tenant resolution, CLI (new-tenant | resolve | validate | demo). Composes kernel + capabilities + packs from a tenant spec.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/capability-billing": "workspace:*",
    "@repo/capability-messaging": "workspace:*",
    "@repo/capability-quoting": "workspace:*",
    "@repo/capability-sourcing": "workspace:*",
    "@repo/kernel": "workspace:*",
    "@repo/pack-jurisdiction-es-es": "workspace:*",
    "@repo/pack-vertical-construction-reformas": "workspace:*",
    "yaml": "^2.6.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^22.15.3",
    "eslint": "^9.39.1",
    "tsx": "^4.19.2",
    "typescript": "5.9.2",
    "vitest": "^3.0.5"
  }
}
